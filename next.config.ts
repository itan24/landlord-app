import type { NextConfig } from "next";

/**
 * Next.js Configuration
 * 
 * Main configuration file for the Next.js application.
 * Contains build settings, environment variables, and other Next.js options.
 * 
 * @see https://nextjs.org/docs/app/api-reference/file-conventions/next-config
 */
const nextConfig: NextConfig = {
  // Add configuration options here as needed
  // Example: experimental features, redirects, rewrites, etc.
};

export default nextConfig;
